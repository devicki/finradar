"""
finradar.api
~~~~~~~~~~~~

FastAPI router assembly for the FinRadar API.

Sub-packages:
- routes/   — individual route modules (news, search, feed)
- deps.py   — shared FastAPI dependencies (DB session, pagination)
"""
